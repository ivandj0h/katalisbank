#!/usr/bin/env node

"use strict";

import {
  staticWelcomeScreenValue,
  staticWelcomeScreenValueTwo,
} from "../helpers/static_val.js";

const run = async () => {
  // show script introduction
  staticWelcomeScreenValue();
  // ask questions
  // create the file
  // show success message
};

run();
